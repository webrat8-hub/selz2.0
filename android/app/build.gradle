plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace "com.example.selz_monitor"
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.example.selz_monitor"
        minSdkVersion 21
        targetSdkVersion 34
        // Hardcode ini biar nggak ada error 'Value is null'
        versionCode 1
        versionName "1.0"
    }
    
    // ... sisa kodenya biarin aja ...
}
